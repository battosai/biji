CSS Styling

Using a flexbox and setting height/width to 100% doesn't take up 100% of the *remaining space*, it tries to take up 100% of the space it *can*.

If the other elements in the same parent container are flexboxes, the space will be eaten up by the 100%-er.